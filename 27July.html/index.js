function commaSep(str){
    let a = ""
       for(i=0;i<str.length;i++){
         a = a + "," +str[i]
       }
        return a.slice(1)
        {
        
       //return str.split(" ").join(",");
     }
  }
     console.log(commaSep('aircampus'))
  